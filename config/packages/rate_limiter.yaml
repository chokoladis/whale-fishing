framework:
    rate_limiter:
        register:
            policy: 'sliding_window'
            limit: 4
            interval: '10 minutes'
        password_restore:
            policy: 'sliding_window'
            limit: 4
            interval: '30 minutes'
        coin_get:
            policy: 'sliding_window'
            limit: 5
            interval: '1 minute'
        api:
            policy: 'token_bucket'
            limit: 20
            rate: { interval: '1 minute' }

